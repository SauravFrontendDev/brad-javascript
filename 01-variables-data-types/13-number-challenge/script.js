const x = Math.ceil(Math.random() * 100);
const y = Math.ceil(Math.random() * 50);

console.log(x, y);

const sum = x + y;
const difference = x - y;
const product = x * y;
const quotiennt = x / y;
const remainder = x % y;

console.log(`${x} + ${y} = ${sum}`);
console.log(`${x} - ${y} = ${difference}`);
console.log(`${x} * ${y} = ${product}`);
console.log(`${x} / ${y} = ${quotiennt}`);
console.log(`${x} % ${y} = ${remainder}`);
